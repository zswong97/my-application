<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="student.inti.assignment">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Task.TaskMainFragment"></activity>
        <activity android:name=".SignUp" />
        <activity android:name=".Task.post_task"/>
        <activity android:name=".Task.TaskFragment2"/>
        <activity android:name=".Exam.ExamFillFragment"/>
        <activity android:name=".Exam.post_exam"/>
        <activity android:name=".photonotes.Upload_notes"/>
        <activity android:name=".photonotes.Photo_notes_mainpage"/>
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>